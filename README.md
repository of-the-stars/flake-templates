# of-the-star's Flake Templates

These are my custom Nix Flake templates, using the tools that I like to use.

The default template output is just a very basic flake skeleton, and each folder contains its own flake template that can be accessed via the `#template` syntax appended to the repo path.
